syntax = "proto3";

package iot;

enum SensorType {
    UNKNOWN = 0;
    TEMPERATURE = 1;
    HUMIDITY = 2;
    PRESSURE = 3;
}

message SensorReading {
    string id = 1;
    SensorType type = 2;
    double value = 3;
}